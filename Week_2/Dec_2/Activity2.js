
function exec() {
    console.log("timer done");
}

setTimeout(exec, 3000);


/**
 * Which one of the above functions is a HOF ? setTimeout
 * Which one of the above functions is a Callback ? exec
 */

