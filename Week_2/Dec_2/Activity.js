const arr = [1,2,3,4,5];


arr.forEach(function printer(value) { console.log("Value is", value); });

/**
 * Which one of the above functions is a HOF ? forEach
 * Which one of the above functions is a Callback ? printer
 */

